name: Test Skraepper

on:
  workflow_dispatch: # Allows manual triggering

jobs:
  test:
    runs-on: windows-latest

    steps:
      # Step 1: Check out the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up Python (latest version)
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x' # Use the latest stable Python 3.x version
          architecture: 'x64'

      # Step 3: Create and activate virtual environment
      - name: Create and activate virtual environment
        shell: pwsh
        run: |
          python -m venv .venv
          .\.venv\Scripts\Activate.ps1

      # Step 4: Install dependencies
      - name: Install dependencies
        shell: pwsh
        run: |
          pip install -r requirements.txt

      # Step 5: Install test-only packages
      - name: Install test-only packages
        shell: pwsh
        run: |
          pip install pyautogui pyperclip

      # Step 6: Install Chromium for Playwright
      - name: Install Chromium
        shell: pwsh
        run: |
          playwright install chromium

      # Step 7: Run test script (allow failure)
      - name: Run test script
        shell: pwsh
        env:
          TEST_SKRAEPPER: 1 # Set the environment variable
        run: |
          python test_playwright_gui.py

      # Step 8: Debug - List contents of current working directory
      - name: List contents of current working directory
        shell: pwsh
        run: |
          Write-Host "Contents of current working directory:"
          ls

      # Step 9: Debug - List contents of ./tests directory
      - name: List contents of ./tests directory
        shell: pwsh
        run: |
          Write-Host "Contents of ./tests directory:"
          ls ./tests